use super::*;

impl GameState {
    /// Checks whether the goal has been reached
    pub fn check_goal(&mut self) {
        // let bindings = self.graph_link.bindings();
        // let constraints = category::constraint::category_to_constraints(&self.goal_category.inner);

        // if category::find::find_candidates(&constraints, bindings, &self.fact_category.inner)
        //     .map(|mut candidates| candidates.next().is_some())
        //     .unwrap_or(false)
        // {
        //     //  The goal has been reached
        //     println!("Hooray! Goal reached!");
        // }
        todo!()
    }
}
